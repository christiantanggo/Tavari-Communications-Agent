'use client';

/**
 * Re-export for stale dev caches that still resolve this path after the move to
 * @/components/PartnerReferralLanding — safe to remove after deleting .next.
 */
export { default } from '@/components/PartnerReferralLanding';
